-- setups: back throw far, back throw close, forward throw, cr mk, shoryu 
-- practice mashing
local menu, menu_items, training, settings
local function init()
   menu = require("src.ui.menu")
   menu_items = require("src.ui.menu_items")
   training = require("src.ui.training")
   settings = require("src.settings")
end

local denjin_menu = {}
local function create_menu()

end

local function update_unblockables_items(opponent_changed)
   
end
